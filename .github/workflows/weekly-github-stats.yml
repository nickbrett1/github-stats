name: weekly-github-stats

on:
  schedule:
    - cron: "30 5 * * 1" # Mondays 05:30 UTC
  workflow_dispatch:
  push:
    branches: [main]
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compute weekly stats
        env:
          GH_TOKEN: ${{ secrets.GH_STATS_TOKEN }}
        run: python .github/scripts/weekly_stats.py --days 7 --out stats/weekly.json
      - name: Commit and push
        run: |
          git config user.name "github-stats-bot"
          git config user.email "actions@users.noreply.github.com"
          git add stats/weekly.json
          if git diff --cached --quiet; then
            echo "no changes to stats"
          else
            git commit -m "chore: refresh weekly github stats"
            git push
          fi
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
  deploy:
    needs: stats
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
